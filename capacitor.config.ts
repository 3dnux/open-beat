import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.openbeat.librero',
  appName: 'Librero Biónico',
  webDir: 'dist/android-www'
};

export default config;
